/**
 * Runs before every test file. `@testing-library/jest-dom` adds matchers like
 * `toBeInTheDocument()` so expectations read closer to how users see the UI.
 */
import "@testing-library/jest-dom";
